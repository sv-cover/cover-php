<h1><?=__('Werkgroep verwijderen') ?></h1>
<div class="messageBox">
	<form method="post" action="<?=markup_format_attribute($controller->link_to_delete($iter))?>" class="vertical-form">
		<p><?=sprintf(__('Weet je heel zeker dat je de werkgroep <em>%s</em> wil verwijderen?'),
			markup_format_text($iter->get('naam')))?></p>
		<div>
			<button type="submit" class="button"><?=__('Verwijder werkgroep') ?></button>
			<a href="<?=markup_format_attribute($controller->link_to_read($iter))?>"><?=__('Terug naar werkgroep')?></a>
		</div>
	</form>
</div>
