{% extends '@layout/layout.twig' %}

{% block title __('Order books') ~ ' – ' ~ parent() %}

{% block content %}
<h1 class="title">{{ __('Order books') }}</h1>

<div class="content">
	<p>To order books with a 10% discount, you need to be a member of Cover.</p>
	<div>
		<a href="sessions.php?view=login&amp;referrer=boeken.php" data-placement-selector="modal" data-partial-selector="#login-form" class="button is-fullwidth is-primary">{{ __('Log in') }}</a>
		<div class="divider is-fullwidth">{{ __('or') }}</div>
		<a href="lidworden.php" class="button is-fullwidth">{{ __('Become a member') }}</a>
	</div>
</div>
{% endblock %}