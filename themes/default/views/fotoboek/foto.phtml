<h1><?= __('Fotoboek') . ' :: ' . __('Foto') ?></h1>

<div class="messageBox">

<?php
	$this->render_partial('path', array('iter' => $book, 'current' => $iter, 'model' => $model));
	$this->render_partial('photo', compact('model', 'iter', 'book'));
	$this->render_partial('slides', compact('model', 'iter', 'book'));

	echo $reacties;
?>

</div>
