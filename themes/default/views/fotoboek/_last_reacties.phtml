<?php if (get_auth()->logged_in()):
	$model = get_model('DataModelFotoboekReacties');
?>
<div class="bar_sep"><?= __('Laatste reacties') ?></div>
<?php
	foreach ($model->get_latest(10) as $iter) {
		$controller = new ControllerFotoboekReacties($iter->getIter('foto', 'DataIterPhoto'));
		View::byName('fotoboekreacties', $controller)->single(compact('model', 'iter'));
	}
?>
<?php endif ?>