{% extends '@layout/layout.twig' %}

{% block content %}
<h1>{{ __('Membership application form') }}</h1>
<div class="messageBox">
	<p>{{ __('Your email address is already known to us, which means you are already in our member administration! In this case it is easier to quickly contact the secretary of Cover via secretary@svcover.nl.') }}</p>
	<div class="call-to-action-or-block">
		<a href="{{ link('sessions.login', referer=global.server.REQUEST_URI) }}" data-placement-selector="modal" data-partial-selector="#login-form" class="button login">{{ __('Log in') }}</a>
		<span class="separator">{{ __('or') }}</span>
		<a href="wachtwoordvergeten.php" class="button reset-password">{{ __('Reset your password') }}</a>
	</div>
</div>
{% endblock %}