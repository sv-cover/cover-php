<h2 class="title is-5">
	<a href="{{ result.absolute_url|e('html_attr') }}">{{ result.title }}</a>
	<small>Wiki, {{ __N('%d hit', '%d hits', result.score) }}</small>
</h2>
<p class="content">{{ result.snippet|raw }}</p>
