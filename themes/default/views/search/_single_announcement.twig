<h2>
	<a href="{{ result.absolute_url|e('html_attr') }}">{{ result.subject }}</a>
	<small>{{ __('Mededeling van %s uit %d')|format(
		'<a href="commissies.php?commissie=%s">%s</a>'|format(
			result.getIter('committee').login|e('url'),
			result.getIter('committee').naam|e('html')),
			result.created_on|date('Y'))|raw }}</small>
</h2>
<p>{{ result.message|strip_markup|e('html')|excerpt(query_parts, 50)|raw }}</p>
