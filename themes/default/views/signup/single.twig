{% extends '@layout/layout.twig' %}

{% block title __('Formulier') ~ ' :: ' ~ parent() %}

{% block content %}
<h1>{{ __('Formulier') }}</h1>
<div class="messageBox">
	<form action="" method="post" class="vertical-form">
		{% for field in iter.fields %}
			<div>
				{{ field.widget.render(view)|raw }}
			</div>
		{% endfor %}
	</form>
</div>
{% endblock %}
