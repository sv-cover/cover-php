<div>
	{{ html_label('Address', name ~ '-address', errors=errors, required=configuration.required) }}
	{{ html_input_text(name ~ '-address', data, errors=errors, required=configuration.required, placeholder='E.g. Nijenborgh 9') }}
	<p class="hint">Street + house number</p>
</div>
<div>
	{{ html_label('Place', name ~ '-city', errors=errors, required=configuration.required) }}
	{{ html_input_text(name ~ '-city', data, errors=errors, required=configuration.required, placeholder='E.g. Groningen') }}
</div>