<div>
	{{ html_label(__('Adres'), name ~ '-address', errors=errors, required=configuration.required) }}
	{{ html_input_text(name ~ '-address', data, errors=errors, required=configuration.required, placeholder=__('Bijv. Nijenborgh 9')) }}
	<p>{{ __('Straatnaam + Huisnummer') }}</p>
</div>
<div>
	{{ html_label(__('Woonplaats'), name ~ '-city', errors=errors, required=configuration.required) }}
	{{ html_input_text(name ~ '-city', data, errors=errors, required=configuration.required, placeholder=__('Bijv. Groningen')) }}
</div>