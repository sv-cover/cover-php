<div class="field-type-email vertical-form">
	<h3 class="field-type">{{ __('E-mail address') }}</h3>
	<fieldset>
		<legend>Required</legend>
		<div>
			{{ html_input_checkbox('required', data, errors=errors) }}
			{{ html_label('Filling in the email field is mandatory', 'required', errors=errors) }}
		</div>
	</fieldset>
</div>