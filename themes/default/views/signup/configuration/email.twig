<div class="field-type-email vertical-form">
	<h3 class="field-type">{{ __('E-mail address') }}</h3>
	<fieldset>
		<legend>Required</legend>
		<div class="checkbox">
			<label>
				{{ html_input_checkbox('required', data, errors=errors, id='field-' ~ name ~ '-required') }}
				{{ __('Filling in the email field is mandatory') }}
			</label>
		</div>
	</fieldset>
</div>