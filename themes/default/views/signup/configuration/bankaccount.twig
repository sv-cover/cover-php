<div class="field-type-bank-account vertical-form">
	<h3 class="field-type">{{ __('Bank account number') }}</h3>
	<fieldset>
		<legend>Required</legend>
		<div class="checkbox">
			<label>
				{{ html_input_checkbox('required', data, errors=errors, id='field-' ~ name ~ '-required') }}
				{{ __('Filling in the IBAN code is mandatory') }}
			</label>
		</div>
	</fieldset>
</div>