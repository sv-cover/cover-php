<div class="field-type-address vertical-form">
	<h3 class="field-type">{{ __('Address field') }}</h3>
	<fieldset>
		<legend>Required</legend>
		<div class="checkbox">
			<label>
				{{ html_input_checkbox('required', data, errors=errors, id='field-' ~ name ~ '-required') }}
				{{ __('Filling in the address is mandatory') }}
			</label>
		</div>
	</fieldset>
</div>